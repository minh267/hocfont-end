// var a;
// console.log(a); 
//  a= 10;
// (a);

// {
//     let const1 = 20;
//     let const2 = "30";
//     const const3 = "JS";
    

//     let a = const1 + const2 ;
// }
//  var có cở chế hosting, const và let không có cơ chế hosting
// k có số ở đầu 
// phân biệt chữ hoa chữ thường
// k đc đặt số trc, chữ trc thì đc 

// x = true, y = true, z = false

// x && y && z → false
// x && y && !z → true
// (x && y) || z → true
// (x && y) || !z → true
// x && (y || z) → true
// x && !(y || z) → false
// x && (y || !z) → true
// x && (!y || z) → false


name = "AAA"; age = 20, isGood = true;

// age + 10 = 30    
// age % 3 = 2
// name + ‘ BBB’ = AAA BBB
// !isGood = false

// name == ‘aaa’ && age >= 20 → false
// name != ‘aaa’ && isGood → true
// !(age < 10) && !isGood → false

// a = 10, b = 20, c = 30, d = '40';

// a + b + c = 60
// a - b / c = 9,333
// a - (b * c) = -590
//d - (a * b) - c = -190
//a + b + c + d = 60 40
// d + a + b + c = 40 60
// d + a - b + c = 40 20 
// a - b + d - c = 
// = '-1040' - 30 = -1070
// d - c + a - b = 0
// a * b + d * c = 1400


//  x = true, y = false, z = 10

// x && y → false
// x && !y → true
// x && z == 10 → true
//(x && z == 10) → true
// !(x && z == 10) → false
// !(x && z == 50) → true

// x && y && z == 10 → false
// x && !y && z == 10 → true
// x || y || z == 10 → true
// (x && y) || z != 10 → false
// !(x && z == 10) || y → false